# Cursor AI Rules для проекта Iwanna

## 📌 ВАЖНО: Читай документацию ПЕРВЫМ ДЕЛОМ

Перед любой работой с проектом ты ОБЯЗАН прочитать:

1. **`docs/prd.md`** - ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ
2. **`docs/logs.md`** - Журнал изменений  
3. **`docs/TROUBLESHOOTING.md`** - Известные проблемы

---

## Архитектура

- React 18 + TypeScript + Vite
- Supabase (PostgreSQL, Auth)
- shadcn/ui + Tailwind CSS
- React Router v6

## Роли

- `admin` - полный доступ
- `vendor` - управление СПА (после активации `active: true`)
- `user` - просмотр и бронирование

## Ключевые таблицы

- `profiles` (role, active)
- `brands` (owner_id → vendor)
- `spas` (brand_id → brand)
- `spa_amenities` (кастомные описания)
- `leads` (заявки)

---

## ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА

### 1. docs/prd.md = Единый источник правды
- Проверяй PRD перед изменениями
- Обновляй PRD после изменений
- Не противоречь PRD

### 2. Фиксируй ВСЕ изменения в docs/logs.md

Формат:
```markdown
## [ДД.ММ.ГГГГ] - Описание
### Изменено: ...
### Причина: ...
### Файлы: ...
```

### 3. Проверяй TROUBLESHOOTING.md перед исправлением багов
- Может проблема известна
- Может есть workaround
- Не дублируй работу

---

## Критические проблемы

🔴 **Медленная загрузка** (>10s)
- Запросы к `profiles` зависают
- См. `docs/TROUBLESHOOTING.md` для деталей

---

## Структура проекта

```
iwanna/
├── docs/              # Документация (ЧИТАЙ!)
├── migrations/        # SQL миграции
├── src/
│   ├── components/    # React компоненты
│   ├── contexts/      # AuthContext и др.
│   ├── hooks/         # useBrands, useCountries
│   ├── services/      # brandService, spaService
│   └── types/         # TypeScript типы
```

---

## Перед коммитом

- [ ] Код работает
- [ ] Обновлен docs/prd.md (если нужно)
- [ ] Добавлена запись в docs/logs.md
- [ ] Понятное сообщение коммита (feat:/fix:/docs:)

---

## Формат коммитов

```
feat: добавлена страница брендов
fix: исправлена загрузка профиля
docs: обновлен PRD
refactor: оптимизирован AuthContext
perf: добавлены индексы БД
```

---

Полная документация: `docs/prd.md`, `docs/README.md`
