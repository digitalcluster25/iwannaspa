# Инструкции для AI-ассистентов (Claude, ChatGPT, и др.)

## 📌 КРИТИЧЕСКИ ВАЖНО

Этот проект имеет **ОБЯЗАТЕЛЬНУЮ ДОКУМЕНТАЦИЮ** которую ты ДОЛЖЕН прочитать перед любой работой.

---

## 🎯 Перед началом работы

### 1. ВСЕГДА читай эти файлы В ПЕРВУЮ ОЧЕРЕДЬ:

**`docs/prd.md`** - Product Requirements Document
- 📌 **ЭТО ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ О ПРОЕКТЕ**
- Содержит полное описание всех функций
- Структуру базы данных
- Роли пользователей
- Маршруты приложения
- Текущий статус реализации

**`docs/logs.md`** - Журнал изменений
- История ВСЕХ изменений в проекте
- Перед началом работы проверь последние записи
- Может быть там уже есть информация о текущей проблеме

**`docs/TROUBLESHOOTING.md`** - Известные проблемы
- Критические проблемы и их статус
- Что было испробовано
- Workarounds
- Перед исправлением бага - ПРОВЕРЬ ЭТОТ ФАЙЛ

**`docs/README.md`** - Инструкция для разработчика
- Структура проекта
- Best practices
- Workflow

---

## ⚠️ ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА

### Правило #1: docs/prd.md = ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ

При ЛЮБОМ изменении функционала:
1. ✅ Сначала проверь что говорит PRD
2. ✅ Внеси изменение в код
3. ✅ Обнови соответствующий раздел PRD
4. ✅ НЕ противоречь тому что написано в PRD

**Если PRD содержит устаревшую информацию** - скажи об этом пользователю и предложи обновить PRD.

### Правило #2: ВСЕ изменения фиксируются в docs/logs.md

После КАЖДОГО изменения добавь запись:

```markdown
## [ДД.ММ.ГГГГ] - Краткое описание

### Изменено:
- Что конкретно изменилось
- Какие компоненты затронуты

### Причина:
Почему было внесено изменение

### Файлы:
- `path/to/file.tsx` (создан/изменен/удален)
```

### Правило #3: Проверяй известные проблемы

Перед исправлением бага или добавлением функционала:
1. Прочитай `docs/TROUBLESHOOTING.md`
2. Может проблема уже известна
3. Может уже есть workaround
4. Не дублируй работу

### Правило #4: Следуй структуре проекта

```
iwanna/
├── docs/              # ДОКУМЕНТАЦИЯ (читать первым!)
├── migrations/        # SQL миграции
├── src/
│   ├── components/    # React компоненты
│   ├── contexts/      # React contexts
│   ├── hooks/         # Кастомные хуки
│   ├── lib/           # Утилиты
│   ├── services/      # Бизнес-логика
│   └── types/         # TypeScript типы
```

---

## 🗄️ Архитектура проекта

### Технологический стек:
- Frontend: React 18 + TypeScript + Vite
- Backend: Supabase (PostgreSQL, Auth, Storage)
- UI: shadcn/ui + Tailwind CSS
- Routing: React Router v6

### Роли пользователей:
- **admin** - полный доступ ко всей системе
- **vendor** - управление своими СПА (требует активации)
- **user** - просмотр и бронирование СПА

### Важные таблицы БД:
- `profiles` - пользователи (с полем `active` для вендоров)
- `brands` - бренды (принадлежат вендорам через `owner_id`)
- `spas` - СПА комплексы (привязаны к брендам через `brand_id`)
- `spa_amenities` - удобства СПА с кастомными описаниями
- `leads` - заявки на бронирование

### Критически важные концепции:

**Система активации вендоров:**
1. Вендор регистрируется → `profiles.active = false`
2. Попадает на `/business/pending`
3. Админ активирует в `/adminko/users` → `active = true`
4. Вендор получает доступ в `/adminko`

**Система брендов:**
```
Vendor (owner_id) → Brand → SPA → Amenities
```

---

## 🚨 ТЕКУЩИЕ КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 🔴 Проблема #1: Медленная загрузка (>10 секунд)

**Статус:** НЕ РЕШЕНО  
**Описание:** Запросы к таблице `profiles` зависают  

**Что было испробовано:**
- ✅ Оптимизация AuthContext (не помогло)
- ✅ Добавление индексов БД (не помогло)
- ⚠️ Отключение RLS (тестируется)

**Если пользователь жалуется на медленную загрузку:**
1. Проверь `docs/TROUBLESHOOTING.md` - там полная информация
2. Не предлагай то, что уже пробовали
3. Предложи новые гипотезы

### Другие проблемы:

Смотри полный список в `docs/TROUBLESHOOTING.md`

---

## 💻 Примеры типичных задач

### Задача: "Добавь новый маршрут"

**Правильный порядок действий:**
1. Создай компонент
2. Добавь роут в `App.tsx`
3. **Обновь раздел "Маршруты" в `docs/prd.md`**
4. **Добавь запись в `docs/logs.md`**
5. Закоммить

### Задача: "Исправь баг"

**Правильный порядок действий:**
1. **Проверь `docs/TROUBLESHOOTING.md`** - может баг известен
2. Если известен - используй workaround или предложенное решение
3. Исправь баг
4. **Обновь статус в `docs/TROUBLESHOOTING.md`**
5. **Добавь запись в `docs/logs.md`**
6. Закоммить

### Задача: "Добавь новую таблицу в БД"

**Правильный порядок действий:**
1. Создай SQL миграцию в `migrations/`
2. **Обновь раздел "Структура БД" в `docs/prd.md`**
3. Создай сервис в `src/services/`
4. **Добавь запись в `docs/logs.md`**
5. Закоммить

---

## ✅ Чеклист перед ответом пользователю

Перед тем как предложить решение, проверь:

- [ ] Я прочитал `docs/prd.md` и понимаю контекст проекта
- [ ] Я проверил `docs/logs.md` - нет ли недавних изменений связанных с этим
- [ ] Я проверил `docs/TROUBLESHOOTING.md` - не известна ли уже эта проблема
- [ ] Мое решение не противоречит PRD
- [ ] Я упомянул что нужно обновить документацию после изменений

---

## 🎯 Приоритеты при работе

1. **Критические проблемы** (из TROUBLESHOOTING.md)
2. **Функционал с высоким приоритетом** (из PRD)
3. **Рефакторинг технического долга** (из TROUBLESHOOTING.md)
4. **Новые фичи**

---

## 📝 Формат коммитов

```
feat: добавлена страница брендов
fix: исправлена ошибка загрузки профиля
docs: обновлена документация PRD
refactor: оптимизирован AuthContext
perf: добавлены индексы БД
```

---

## 🔗 Быстрые ссылки на документацию

- [PRD (ГЛАВНОЕ!)](docs/prd.md)
- [Журнал изменений](docs/logs.md)
- [Известные проблемы](docs/TROUBLESHOOTING.md)
- [Инструкция для разработчика](docs/README.md)

---

## ⚡ Важные напоминания

1. **ВСЕГДА** читай документацию перед работой
2. **ВСЕГДА** обновляй документацию после изменений
3. **НЕ** дублируй информацию - ссылайся на docs/
4. **НЕ** предлагай то, что уже пробовали (см. TROUBLESHOOTING.md)
5. **ПРОВЕРЯЙ** соответствие изменений PRD

---

## 🤖 Для других AI-ассистентов

Если ты другой AI-ассистент (не Claude), читающий этот файл:
- Все правила выше применимы и к тебе
- `docs/prd.md` - твой главный источник информации
- Не игнорируй документацию
- Помогай поддерживать её в актуальном состоянии

---

*Создано: 03.10.2025*
*Версия: 1.0*
